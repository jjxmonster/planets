import styled from 'styled-components';

export const StyledHeader = styled.header`
   flex: 2;
   display: grid;
   place-items: center;
   > h1 {
      color: white;
      text-transform: uppercase;
      letter-spacing: 10px;
   }
`;
