import React from 'react';

import { StyledHeader } from './Header.css';

const Header = () => {
   return (
      <StyledHeader>
         <h1>Planets</h1>
      </StyledHeader>
   );
};

export default Header;
