import styled from 'styled-components';

export const StyledBar = styled.nav`
   width: 100vw;
   height: 50px;
   color: white;
   display: flex;
   align-items: center;
   justify-content: space-around;
`;
